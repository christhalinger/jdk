language: java

dist: xenial

addons:
  apt:
    packages:
      - libx11-dev
      - libxext-dev
      - libxrender-dev
      - libxrandr-dev
      - libxtst-dev
      - libxt-dev

jdk:
    - openjdk13

script:
  - sh configure
  - make images LOG=debug
